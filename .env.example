# Required
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Your Railway public HTTPS URL (enables webhook mode)
PUBLIC_BASE_URL=https://your-app.up.railway.app

# Where state.json is stored — mount a persistent volume here
DATA_DIR=/data

# Port (Railway sets this automatically)
PORT=8000
